[build]
  # Base directory containing the site (the project lives in the `Liquor` folder)
  base = "Liquor"
  publish = "dist"
  command = "npm run build"

[context.production.environment]
  NODE_ENV = "production"

[dev]
  command = "npm run dev"
  port = 5173
